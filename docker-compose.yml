services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: agentdesk
      POSTGRES_PASSWORD: dev
    ports: ["5433:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]

volumes:
  pgdata:
